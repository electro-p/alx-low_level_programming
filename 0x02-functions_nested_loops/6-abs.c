#include "main.h"

/**
 *_abs - Check description
 * Description: returns absolute value of a number
 * @r: An integre input
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-1 * r);
}
