#include "main.h"

/**
 *_abs - Check description
 * Description: returns absolute value of a number
 * @r: An integre input
 * Return: the absolute value of r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-1 * r);
}
